---
- name: Clone Github project
  become: true
  git:
    repo: "https://github.com/IDRISSHACKER/api.camwego.git"
    dest: "./"
    clone: yes
    update: yes

- name: switch to production env
  become: true
  command:
    cmd: sed -i "s/PROD=0/PROD=1/g" ./.env

- name: kill existing service
  become: true
  docker_compose:
    project_src: "./"
    state: absent

- name: Create and start container
  become: true
  docker_compose:
    project_src: "./"
  register: output

- name: Show container logs
  become: true
  debug:
    var: output
...
